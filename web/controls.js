import {t} from './i18n/index.js';
let menuId=0;
const drawWave=(canvas,peaks,progress=0)=>{const w=canvas.clientWidth||208,h=17,dpr=(canvas.ownerDocument?.defaultView?.devicePixelRatio)||1;canvas.width=w*dpr;canvas.height=h*dpr;const g=canvas.getContext('2d');g.scale(dpr,dpr);const styles=getComputedStyle(canvas),muted=styles.getPropertyValue('--muted').trim()||styles.color,played=styles.getPropertyValue('--accent').trim()||muted;const n=peaks.length,step=w/n;for(let i=0;i<n;i++){const bar=Math.max(1,peaks[i]*h);g.fillStyle=(i+1)*step<=progress*w?played:muted;g.fillRect(i*step,(h-bar)/2,Math.max(1,step*.66),bar);}};
const applyRowPlaying=(wave,playing,progress=0)=>{wave.classList.toggle('playing',playing);const path=wave.querySelector('.glyph path');if(path)path.setAttribute('d',playing?'M6 4h3v12H6zM11 4h3v12h-3z':'M7 4l9 6-9 6z');wave.title=playing?t('action.stop'):t('action.play');wave.setAttribute('aria-label',playing?wave.dataset.stopLabel||wave.dataset.playLabel||'':wave.dataset.playLabel||'');const canvas=wave.querySelector('canvas');if(canvas&&wave.dataset.peaks)queueMicrotask(()=>drawWave(canvas,JSON.parse(wave.dataset.peaks),progress));};
class KoeSelect extends HTMLElement{
 constructor(){super();this.attachShadow({mode:'open'});this._value=null;this._disabled=false;this._editing=false;this.active=0;this.uid='choice-'+(++menuId);}
 connectedCallback(){this.style.display='inline-flex';this.shadowRoot.innerHTML=`<style>:host{position:relative;min-width:0;color:var(--ink);font:inherit}button{font:inherit;color:inherit;cursor:pointer}button:focus-visible{outline:2px solid var(--accent);outline-offset:2px}.trigger{display:flex;justify-content:space-between;align-items:center;gap:8px;width:100%;height:100%;min-height:28px;padding:5px 8px;background:var(--surface);border:1px solid var(--line);border-radius:6px;font-size:inherit;text-align:left}.trigger span{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.icon{width:16px;height:16px;flex-shrink:0;color:var(--muted);fill:none;stroke:currentColor;stroke-width:1.6;stroke-linecap:round;stroke-linejoin:round}.trigger:after{content:'';width:5px;height:5px;border-right:1.5px solid var(--muted);border-bottom:1.5px solid var(--muted);transform:rotate(45deg);margin:0 3px 3px 0;flex-shrink:0}.trigger:disabled{opacity:.4;cursor:default}.list{position:fixed;inset:auto;margin:0;padding:4px;background:var(--surface);color:var(--ink);border:1px solid var(--line);border-radius:9px;box-shadow:var(--shadow);min-width:120px;max-height:min(320px,55vh);overflow:auto;scrollbar-width:thin;scrollbar-color:var(--muted) transparent;overscroll-behavior:contain;z-index:1000}.list::-webkit-scrollbar{width:7px}.list::-webkit-scrollbar-track{background:transparent}.list::-webkit-scrollbar-thumb{background:var(--muted);border:2px solid var(--surface);border-radius:8px}.list::-webkit-scrollbar-button{display:none}.list[role=menu]{width:min(340px,calc(100vw - 24px));max-height:min(420px,65vh);padding:6px}.choice-row+.choice-row{margin-top:2px}.item{display:block;border:0;background:transparent;padding:8px 10px;width:100%;text-align:left;white-space:nowrap;border-radius:5px;font-size:12px}.item:hover,.item:focus{background:var(--raised);outline:0}.item[aria-selected=true],.item[aria-checked=true]{background:var(--selected);color:var(--accent)}.item:disabled{opacity:.4}.item .detail{color:var(--muted);font-size:10px;margin-left:7px;font-variant-numeric:tabular-nums}.item.divider{border-top:1px solid var(--line);border-radius:0;margin-top:5px;padding-top:12px}.choice-row{display:grid;grid-template-columns:minmax(0,1fr) auto;align-items:center;column-gap:2px;max-width:min(400px,calc(100vw - 24px))}.choice-row .item{grid-column:1;grid-row:1;min-width:0;width:auto;overflow:hidden;text-overflow:ellipsis}.choice-row .rename{grid-column:1;grid-row:1;min-width:0;width:100%;font:inherit;font-size:12px;color:var(--ink);background:var(--surface);border:1px solid var(--accent);border-radius:5px;padding:5px 7px}.choice-row .row-actions{grid-column:2;grid-row:1;display:flex}.choice-row .row-action{width:28px;height:28px;padding:4px}.row-action{display:grid;place-items:center;flex-shrink:0;border:0;border-radius:5px;background:transparent;color:var(--muted)}.row-action:hover,.row-action:focus-visible{background:var(--raised);color:var(--accent)}.row-action[data-action=delete]:hover,.row-action[data-action=delete]:focus-visible{color:var(--danger)}.row-action:disabled{opacity:.3;cursor:default}.row-action svg{width:16px;height:16px;fill:none;stroke:currentColor;stroke-width:1.6;stroke-linecap:round;stroke-linejoin:round}.choice-row .wave{grid-column:1/-1;grid-row:2;position:relative;display:block;border:0;background:transparent;border-radius:5px;padding:1px 2px 3px;width:100%;height:21px;color:var(--muted)}.wave:hover:not(:disabled),.wave:focus-visible{color:var(--accent)}.wave:disabled{opacity:.35;cursor:default}.wave.playing{color:var(--accent)}.wave canvas{display:block;width:100%;height:17px}.wave .glyph{position:absolute;left:1px;top:50%;translate:0 -50%;width:13px;height:13px;color:var(--accent);fill:currentColor;stroke:none;opacity:0;transition:opacity .12s;pointer-events:none;filter:drop-shadow(0 0 3px var(--surface)) drop-shadow(0 0 2px var(--surface))}.wave:hover:not(:disabled) .glyph,.wave:focus-visible .glyph,.wave.playing .glyph{opacity:1}</style><button part="trigger" class="trigger" type="button" aria-haspopup="listbox" aria-expanded="false" aria-controls="${this.uid}"><span></span></button><div class="list" id="${this.uid}" role="listbox" popover="auto"></div>`;
 this.trigger=this.shadowRoot.querySelector('.trigger');const symbol=this.getAttribute('icon')&&document.getElementById(this.getAttribute('icon'));if(symbol){const icon=document.createElementNS('http://www.w3.org/2000/svg','svg');icon.setAttribute('class','icon');icon.setAttribute('viewBox',symbol.getAttribute('viewBox'));icon.setAttribute('aria-hidden','true');icon.innerHTML=symbol.innerHTML;this.trigger.prepend(icon);}this.list=this.shadowRoot.querySelector('.list');this.trigger.onclick=()=>this.list.matches(':popover-open')?this.close():this.open();this.list.addEventListener('toggle',()=>{this.trigger.setAttribute('aria-expanded',String(this.list.matches(':popover-open')))});
 this.trigger.onkeydown=e=>{if(['ArrowDown','ArrowUp','Enter',' '].includes(e.key)){e.preventDefault();e.stopPropagation();this.open(e.key==='ArrowUp'?-1:0)}};
 this.list.onkeydown=e=>{if(e.target.tagName==='INPUT')return;e.stopPropagation();const buttons=[...this.list.querySelectorAll('button:not(:disabled)')],i=buttons.indexOf(this.shadowRoot.activeElement);let next;if(e.key==='ArrowDown')next=(i+1)%buttons.length;else if(e.key==='ArrowUp')next=(i-1+buttons.length)%buttons.length;else if(e.key==='Home')next=0;else if(e.key==='End')next=buttons.length-1;else if(e.key==='Escape'){e.preventDefault();this.close();return;}else if(e.key.length===1){next=buttons.findIndex((b,j)=>j>i&&b.textContent.toLowerCase().startsWith(e.key.toLowerCase()));if(next<0)next=buttons.findIndex(b=>b.textContent.toLowerCase().startsWith(e.key.toLowerCase()));}if(next>=0){e.preventDefault();buttons[next]?.focus();}};
 this.observer=new MutationObserver(()=>this.render());this.observer.observe(this,{childList:true,subtree:true,characterData:true,attributes:true,attributeFilter:['aria-label','title','disabled','data-display-label']});this.render();}
 disconnectedCallback(){this.observer?.disconnect();}
 get options(){return [...this.querySelectorAll(':scope > option')];}
 get selectedOptions(){return this.options.filter(o=>o.value===this.value);}
 get value(){return this._value??this.options.find(o=>o.hasAttribute('selected'))?.value??this.options[0]?.value??'';}
 set value(v){this._value=String(v);this.render();}
 get disabled(){return this._disabled;}
 set disabled(v){this._disabled=Boolean(v);this.render();}
 add(option){this.append(option);}
 render(){if(!this.trigger)return;if(this._editing)return;const options=this.options;let selected=options.find(o=>o.value===this.value);if(!selected&&this._value!==''){this._value=null;selected=options.find(o=>o.hasAttribute('selected'))||options[0];}
 this.trigger.querySelector('span').textContent=this.getAttribute('data-display-label')||selected?.textContent||'';this.trigger.disabled=this.disabled||this.hasAttribute('disabled');this.trigger.setAttribute('aria-label',this.getAttribute('aria-label')||this.getAttribute('title')||selected?.textContent||'');this.list.setAttribute('aria-label',this.trigger.getAttribute('aria-label'));
 const actionMenu=options.some(o=>o.dataset.actions);this.list.role=actionMenu?'menu':'listbox';this.trigger.setAttribute('aria-haspopup',actionMenu?'menu':'listbox');
 this.list.replaceChildren(...options.map(o=>{
  const wanted=o.dataset.actions?o.dataset.actions.split(','):[];
  const b=document.createElement('button');b.className='item'+(o.hasAttribute('data-divider')?' divider':'');b.type='button';b.role=actionMenu?'menuitemradio':'option';b.disabled=o.disabled;b.textContent=o.textContent;
  if(o.dataset.detail){const small=document.createElement('small');small.className='detail';small.textContent=o.dataset.detail;b.append(small);}
  b.dataset.value=o.value;b.setAttribute(actionMenu?'aria-checked':'aria-selected',String(o===selected));
  if(!wanted.length){b.onclick=()=>{this.value=o.value;this.close();this.dispatchEvent(new Event('change',{bubbles:true}));};return b;}
  const row=document.createElement('div');row.className='choice-row';row.role='group';row.setAttribute('aria-label',o.textContent);row.append(b);
  b.onclick=()=>{if(wanted.includes('rename')&&o===selected){this.editName(o,b);return;}this.value=o.value;this.close();this.dispatchEvent(new Event('change',{bubbles:true}));};
  if(wanted.includes('rename')&&o===selected)b.title=t('action.rename');
  if(o.dataset.peaks&&wanted.includes('play')){
   const wave=document.createElement('button');wave.type='button';wave.className='wave';wave.role='menuitem';wave.dataset.value=o.value;wave.dataset.rowKey=o.dataset.key||o.value;wave.dataset.peaks=o.dataset.peaks;wave.dataset.playLabel=t('action.label',{name:o.textContent,action:t('action.play')});wave.dataset.stopLabel=t('action.label',{name:o.textContent,action:t('action.stop')});
   wave.disabled=o.disabled||o.dataset.disabledActions?.split(',').includes('play');
   wave.setAttribute('aria-label',wave.dataset.playLabel);wave.title=t('action.play');
   wave.innerHTML='<canvas></canvas><svg class="glyph" viewBox="0 0 20 20" aria-hidden="true"><path d="M7 4l9 6-9 6z"/></svg>';
   wave.onclick=()=>this.dispatchEvent(new CustomEvent('optionaction',{bubbles:true,detail:{value:o.value,action:'play'}}));
   row.append(wave);queueMicrotask(()=>drawWave(wave.querySelector('canvas'),JSON.parse(o.dataset.peaks)));
   const playing=this._rowPlaying?.[wave.dataset.rowKey];if(playing)applyRowPlaying(wave,playing.playing,playing.progress);
  }
  const actions={download:{label:t('action.download'),path:'M10 3v10m-4-4 4 4 4-4M4 13v4h12v-4'},delete:{label:t('action.delete'),path:'M3 5h14M8 5V3h4v2M5 5l1 12h8l1-12M8 8v6m4-6v6'}};
  const bar=document.createElement('div');bar.className='row-actions';
  for(const action of wanted){
   const spec=actions[action];if(!spec)continue;
   const button=document.createElement('button');button.className='row-action';button.type='button';button.role='menuitem';button.dataset.value=o.value;button.dataset.action=action;button.disabled=o.disabled||o.dataset.disabledActions?.split(',').includes(action);button.setAttribute('aria-label',t('action.label',{name:o.textContent,action:spec.label}));button.title=spec.label;
   button.innerHTML='<svg viewBox="0 0 20 20" aria-hidden="true"><path d="'+spec.path+'"/></svg>';
   button.onclick=()=>{this.close();this.dispatchEvent(new CustomEvent('optionaction',{bubbles:true,detail:{value:o.value,action}}));};bar.append(button);
  }
  row.append(bar);
  return row;
 }));}
 editName(o,item){if(this._editing)return;this._editing=true;const row=item.closest('.choice-row');if(!row){this._editing=false;return;}
  const input=document.createElement('input');input.className='rename';input.type='text';input.maxLength=60;input.value=o.textContent;input.setAttribute('aria-label',t('action.rename'));
  let done=false;const finish=commit=>{if(done)return;done=true;this._editing=false;const name=input.value.trim();if(commit&&name&&name!==o.textContent){o.textContent=name;this.dispatchEvent(new CustomEvent('optionrename',{bubbles:true,detail:{value:o.value,key:o.dataset.key||'',name}}));}this.render();this.list.querySelector(`.item[data-value="${CSS.escape(o.value)}"]`)?.focus();};
  input.onkeydown=e=>{e.stopPropagation();if(e.key==='Enter'){e.preventDefault();finish(true);}else if(e.key==='Escape'){e.preventDefault();finish(false);}};
  input.onblur=()=>finish(true);
  row.replaceChild(input,item);input.focus();input.select();
 }
 setRowPlaying(key,playing,progress=0){(this._rowPlaying??={})[key]={playing,progress};const wave=this.shadowRoot.querySelector(`.wave[data-row-key="${CSS.escape(key)}"]`);if(wave)applyRowPlaying(wave,playing,progress);}

 open(direction=0){if(this.disabled)return;this.render();const rect=this.getBoundingClientRect();this.list.showPopover();const box=this.list.getBoundingClientRect();this.list.style.left=Math.max(6,Math.min(rect.left,innerWidth-box.width-6))+'px';this.list.style.top=(rect.bottom+box.height+6<innerHeight?rect.bottom+5:Math.max(6,rect.top-box.height-5))+'px';const buttons=[...this.list.querySelectorAll('button:not(:disabled)')];const selected=buttons.findIndex(b=>b.dataset.value===this.value);buttons[direction<0?buttons.length-1:Math.max(0,selected)]?.focus();}
 close(){this.list.hidePopover();this.trigger.focus();}
}
customElements.define('koe-select',KoeSelect);
