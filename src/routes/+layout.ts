// The studio measures the microphone and stores takes in the browser; every API call is
// made from the page, so the app renders on the client only.
export const ssr = false;
export const prerender = false;
